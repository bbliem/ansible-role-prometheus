prometheus_config_dir: /usr/local/etc/prometheus
prometheus_docker_image: prom/prometheus:v2.43.0
prometheus_blackbox_exporter_docker_image: prom/blackbox-exporter:v0.23.0
prometheus_traefik_router_rule: Host(`{{ prometheus_hostname }}`)
prometheus_web_external_url: https://{{ prometheus_hostname }}
